# Add project-specific ProGuard rules here.
# By default, this file is empty; the release build type currently
# ships with minification disabled (isMinifyEnabled = false), so
# these rules are not applied yet. Flip isMinifyEnabled to true in
# app/build.gradle.kts once you're ready to shrink/obfuscate, and
# add any keep-rules the Play Billing Library needs at that point.
